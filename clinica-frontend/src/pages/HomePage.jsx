import React from 'react';
import Layout from '../components/Layout';
import { Typography } from '@mui/material';

const HomePage = () => {
  return (
    <Layout>
      <Typography variant="h4">Bem-vindo à Clínica Online</Typography>
    </Layout>
  );
};

export default HomePage;